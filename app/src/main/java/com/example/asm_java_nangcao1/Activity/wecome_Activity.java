package com.example.asm_java_nangcao1.Activity;import androidx.appcompat.app.AppCompatActivity;import android.content.Intent;import android.content.SharedPreferences;import android.os.Bundle;import android.util.Log;import android.view.View;import android.widget.LinearLayout;import android.widget.TextView;import com.example.asm_java_nangcao1.R;import com.example.asm_java_nangcao1.service.TaskService;public class wecome_Activity extends AppCompatActivity {    private TextView            Wecome_tv_Context,            Wecome_tv_title;    private LinearLayout            Wecome_llout_btn_getStarted;    Intent service;    SharedPreferences checkService ,shareAcout;    @Override    protected void onCreate(Bundle savedInstanceState) {        setContentView(R.layout.activity_wecome);        service = new Intent(this, TaskService.class);        wecome_Activity.this.startService(service);        anhXa();        batSuKien();        super.onCreate(savedInstanceState);        int check = checkService.getInt("key_service", 0);        SharedPreferences.Editor editor = checkService.edit();        if (check == 0) {            editor.putInt("key_service", 1);            editor.apply();        }        editor.putInt("key_service", 0);        editor.apply();        kiemTraDaCoTaiKhoanChua();    }    private void kiemTraDaCoTaiKhoanChua() {        String username = shareAcout.getString("key_userName", "");        String password = shareAcout.getString("key_password", "");        if (!(username.equals("") && password.equals(""))) {            Intent intent = new Intent(wecome_Activity.this, sanPham_Activity.class);            startActivity(intent);        }    }    private void batSuKien() {        Wecome_llout_btn_getStarted.setOnClickListener(new View.OnClickListener() {            @Override            public void onClick(View view) {                Intent intent = new Intent(wecome_Activity.this, Login_Activity.class);                startActivity(intent);            }        });    }    private void anhXa() {        shareAcout = getSharedPreferences("AccountSharedPreferences", MODE_PRIVATE);        checkService = getSharedPreferences("SharedPrService", MODE_PRIVATE);//        TextView        Wecome_tv_Context = findViewById(R.id.wecome_tv_Context);        Wecome_tv_title = findViewById(R.id.wecome_tv_title);//        LinearLayout        Wecome_llout_btn_getStarted = findViewById(R.id.wecome_llout_btn_getStarted);    }}